version: '3.7'

services:
  my_portfolio:
    container_name: my_portfolio
    image: docker.adhirven.com/my_portfolio:0.0.1
    restart: unless-stopped
    networks:
      - proxy_net
      # deploy:
      # resources:
      # limits:
      # cpus: 1
      # memory: 256M

networks:
  proxy_net:
    external: true
